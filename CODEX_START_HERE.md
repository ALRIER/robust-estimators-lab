# Codex: start here

You are implementing a thesis-defense dashboard under a one-week deadline.

## First task
1. Read `AGENTS.md` completely.
2. Read `docs/MASTER_PLAN.md`, `docs/DATA_MANIFEST.md`, `docs/SCIENTIFIC_GUARDRAILS.md`, and `docs/ACCEPTANCE_CRITERIA.md`.
3. Inspect `design/mockups/layer1_build_problem.png` and `design/mockups/layer2_ga_search.png`.
4. Run `python scripts/build_processed_data.py` and `pytest -q`.
5. Run the current app and confirm all four routes load.
6. Implement only **Milestone 1 / Layer 1** using `prompts/01_LAYER1_BUILD_PROBLEM.md`.
7. Do not begin Layer 2 until Layer 1 acceptance criteria pass.

## Important
The research archives are references, not working files. If you need to inspect them, extract to a temporary directory or copy individual files; never modify the archives.
